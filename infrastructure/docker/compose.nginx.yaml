services:
  gateway:
    image: nginx
    env_file: ./../nginx/.env
    volumes:
      - ./../nginx/templates:/etc/nginx/templates
    networks:
      - flox-net
    ports:
      - 80:80
    restart: always

networks:
  flox-net:
    external: true
